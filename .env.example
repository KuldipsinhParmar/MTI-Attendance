# ---------------------------------------------------------------
# MTI Attendance System — Environment Configuration
# Copy this file to .env and update values before running
# ---------------------------------------------------------------

CI_ENVIRONMENT = development

# App
app.baseURL = 'http://localhost:8080/'

# Database
database.default.hostname = db
database.default.database = mti_attendance_db
database.default.username = mti_user
database.default.password = mti_pass
database.default.DBDriver = MySQLi
database.default.port     = 3307

# Geofence default radius (meters)
app.defaultGeofenceRadius = 50
